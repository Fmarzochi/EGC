language: "en-US"

reviews:
  profile: "assertive"
  request_changes_workflow: false
  high_level_summary: true
  review_status: true
  auto_review:
    # Reviews every PR except the maintainer's own (reviewed by the local
    # squad before push, so no CodeRabbit credits are spent on them) and
    # bot PRs. ignore_usernames takes precedence over all other settings.
    # Native auto_review is used instead of a workflow-posted
    # "@coderabbitai review" because CodeRabbit ignores commands posted by
    # github-actions[bot], which left every PR unreviewed.
    enabled: true
    drafts: false
    ignore_usernames:
      - "Fmarzochi"
      - "dependabot[bot]"

  path_filters:
    # Exclude pure prompt/content markdown files from the 150-file review limit.
    # These files contain no executable code and only need prose quality checks,
    # which are handled separately. Keeping them in scope would exhaust the
    # per-PR file budget on style-only PRs.
    - "!skills/**"
    - "!.agents/skills/**"
    - "!agents/**"
    - "!commands/**"
    - "!examples/**"
    - "!rules/**"
    - "!memory/**"
    - "!.cursor/skills/**"
    - "!.kiro/skills/**"
    - "!.opencode/prompts/**"

    # Always review code, configuration, and infrastructure.
    - "scripts/**"
    - "mcp/**"
    - "src/**"
    - "tests/**"
    - "hooks/**"
    - "schemas/**"
    - "docs/**"
    - "manifests/**"
    - ".github/**"
    - "*.json"
    - "*.yaml"
    - "*.yml"
    - "*.ts"
    - "*.js"
    - "*.sh"
    - "*.ps1"
    - "README.md"
    - "CHANGELOG.md"
    - "AGENTS.md"
    - "CLAUDE.md"
    - "GEMINI.md"
